import { type FC } from "react";
import { type Action } from "@buf/penumbra-zone_penumbra.bufbuild_es/penumbra/core/transaction/v1/transaction_pb";

const TxActionView : FC<Action> = ( action ) => {
  return (
    <div>
    </div>
  );
};

export default TxActionView;
